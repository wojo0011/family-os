# Google OAuth 2.0 Web client ID. No client secret belongs in a browser application.
VITE_GOOGLE_CLIENT_ID=

# Optional browser-side OpenWeather key. If omitted, Family OS uses Open-Meteo.
VITE_OPENWEATHER_API_KEY=
